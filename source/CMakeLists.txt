cmake_minimum_required(VERSION 3.0)
if (__CLIENT__)
	project(client.startup)
	add_definitions("-D__CLIENT__")
elseif(__SERVER__)
	project(server.startup)
	add_definitions("-D__SERVER__")
endif()
include_directories(${__INCLUDE__})
add_subdirectory(com.startup)
