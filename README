file(READ FileList.txt STARTUP_LIST)

LOCAL_SRC_FILES := $(shell cat $(LOCAL_PATH)/list.txt)

cmake_minimum_required(VERSION 3.0)

file(STRINGS FileList.txt STARTUP_LIST)

#set(STARTUP_LIST "First.cpp Main.cpp")

separate_arguments(STARTUP_LIST)

message(${STARTUP_LIST})

add_executable(${PROJECT_NAME} ${STARTUP_LIST})